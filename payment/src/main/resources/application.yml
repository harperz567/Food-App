server:
  port: 9095

spring:
  profiles:
    active: local

  application:
    name: PAYMENT-SERVICE
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

stripe:
  secret:
    key: sk_test_replace_with_your_actual_key